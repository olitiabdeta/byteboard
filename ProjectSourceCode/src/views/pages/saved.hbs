<h2>Saved Recipes</h2>
<!--<h2 class="text-center">Saved</h2>
<div class="row" id="discover">
    {{#each results}}
        <div class="col-sm-3 mb-3 mb-sm-0 ms-5 mt-4">
          <div class="card" style="width: 18rem;" id="saved-card-1">
            <img src="{{#if image}} {{image}} {{else}} 'concert.jpeg' {{/if}}" class="card-img-top" alt="{{name}}" style="width: 18rem; height: 12rem;">
            <div class="card-body">
              <h5 class="card-title">{{name}}</h5>
              <p class="text-center">{{date}} at {{time}}</p>
              <a href="{{bookingUrl}}" class="btn btn-primary">Book Now</a>
            </div>
          </div>
        </div>
    {{/each}}
</div>-->